vagrant-kill-server-warning plugin
===============================

Use this to disable the "Vagrant has detected a custom Vagrant server in use for downloading box files" warning

# How to install
1. run `vagrant plugin install vagrant-kill-server-warning`
2. `vagrant up`
